apiVersion: database.azure.crossplane.io/v1alpha3
kind: PostgreSQLServerVirtualNetworkRule
metadata:
  name: rdx-psql-vnrule
  annotations:
    argocd.argoproj.io/sync-wave: "5"
spec:
  providerConfigRef:
    name: default
  resourceGroupNameRef:
    name: rg-crossplane
  serverNameRef:
    name: rdx-psqldb
  properties:
    virtualNetworkSubnetIdRef:
      name: sub-crossplane
